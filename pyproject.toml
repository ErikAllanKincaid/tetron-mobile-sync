[project]
name = "tetron-mobile-sync"
version = "0.1.0"
description = "Spec tree for the tetron-mobile-sync GPL-3.0 Android photo-backup addon"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "libspec",
]

[tool.uv.sources]
libspec = { git = "https://github.com/drhodes/libspec", tag = "v10.3.1" }